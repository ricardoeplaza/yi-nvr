# VCS & CI (no hace falta en la imagen)
.git
.github

# Dependencias: se reconstruyen con `npm ci` DENTRO de la imagen.
# NUNCA copiar el node_modules de la máquina de dev: better-sqlite3 es nativo
# y el binding de Windows/Linux/ARM no es portable (ver D6, docs/ARCHITECTURE.md).
node_modules
apps/api/node_modules
apps/frontend/node_modules

# Builds / cachés de dev
dist
.angular
apps/frontend/dist
coverage
# Build de Angular copiado a mano en dev (lo genera el stage frontend-build)
apps/api/src/public

# Storage: datos reales, NUNCA en la imagen (en Docker son volúmenes: ./data,
# ./incoming, ./recordings; el remoto se mapea al volume de /app/remote_recordings)
data
incoming
recordings
remote_recordings
storage
apps/api/src/storage
*.db
*.db-wal
*.db-shm
*.sqlite

# Secrets / entorno (en Docker los env los pasa docker-compose)
.env
.env.*

# Docs / infra / tooling (no necesarios para la imagen; cameras.json se monta
# en runtime vía docker-compose, igual que la config de mosquitto/go2rtc)
docs
infra
README.md
README.es.md
docker-compose.yml

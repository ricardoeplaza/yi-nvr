# Dependencies
node_modules/

# Environment / secrets (never commit)
.env
.env.*
!.env.example

# Camera config (real LAN IPs / credentials, never in git — use the .example files)
apps/api/src/config/cameras.json
infra/go2rtc/go2rtc.yaml

# Storage: videos, processed media, SQLite DB (real data, never in git)
apps/api/src/storage/
storage/
*.db
*.db-wal
*.db-shm
*.sqlite

# Builds / generated
dist/
.angular/

# OpenCode Agent
.opencode

# Logs
*.log
npm-debug.log*

# OS / editor
Thumbs.db
.DS_Store
